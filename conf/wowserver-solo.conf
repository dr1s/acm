# Name prefix for Docker images (e.g. playerbots/ac-wotlk-worldserver).
NAME=solo

# Main repository (the base AzerothCore fork).
# Uncomment and edit to use a different upstream:
MAIN_REPO=https://github.com/azerothcore/azerothcore-wotlk.git

# Git repositories to clone into ./modules.
# One URL per line. Lines starting with # are comments.
MODULE_REPO=https://github.com/dr1s/mod-attunement-skip.git
MODULE_REPO=https://github.com/ZhengPeiRu21/mod-individual-progression.git
